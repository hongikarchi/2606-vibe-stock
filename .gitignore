# generated artifacts
out/
/data/
lib/

# but DO commit the web app's data artifacts (React data source — durable + shareable)
!web/public/data/

# project snapshot dirs (local rollback copies)
*-snapshot-*/

# secrets (API keys)
.env

# python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
backups/

# web app build artifacts (source + data committed, build output not)
web/node_modules/
web/dist/
logs/
